# Tell npm which platform to target when resolving optional native binaries.
# This ensures Rollup's linux-x64-gnu prebuilt is selected inside Docker
# instead of the WASM fallback, keeping Vite build times fast.
os=linux
cpu=x64
libc=glibc
